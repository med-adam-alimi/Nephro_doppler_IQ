name: Nephro_doppler
description: A new Flutter project.
publish_to: none  # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.16.0  # Update to a compatible version
  firebase_auth: ^4.11.0  # Update to a compatible version
  cloud_firestore: ^4.17.5  # Corrected indentation here
  intl: ^0.17.0 # Add this line for intl package


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/background.jpeg
    - assets/images/background1.jpg
